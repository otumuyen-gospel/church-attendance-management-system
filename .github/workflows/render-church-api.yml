name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *' # Every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Site
        run: curl -s https://onrender.com
