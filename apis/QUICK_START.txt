╔════════════════════════════════════════════════════════════════╗
║          FACE RECOGNITION FRONTEND - QUICK START                ║
╚════════════════════════════════════════════════════════════════╝

📌 PROBLEM FIXED
  ✗ Before: Camera access blocked on non-localhost devices (HTTP)
  ✓ Now: Works on any device with HTTPS

🚀 HOW TO RUN

  Option 1 (Easiest - Windows):
    cd apis
    .\run_https_server.ps1

  Option 2 (Command line):
    cd apis
    python manage.py runserver_plus 0.0.0.0:8000

  Option 3 (With explicit certificates):
    python manage.py runserver_plus \
      --cert-file certs/localhost.crt \
      --key-file certs/localhost.key \
      0.0.0.0:8000

📱 HOW TO ACCESS

  Same Device:
    https://localhost:8000/user_faces/frontend/

  Another Device (replace IP with yours):
    https://192.168.1.100:8000/user_faces/frontend/

  Find Your IP:
    Windows PowerShell:
      ipconfig
    Look for IPv4 Address (usually 192.168.x.x or 10.x.x.x)

⚠️  BROWSER WARNING

  First visit: "Your connection is not private"
  This is NORMAL - self-signed certificate
  
  Chrome/Edge:
    → Click "Advanced"
    → Click "Proceed to [IP address]"
  
  Firefox:
    → Click "Advanced..."
    → Click "Accept the Risk and Continue"
  
  Safari:
    → Click "Show Details"
    → Click "Visit this website"

✅ TESTING STEPS

  1. Run the startup script or command above
  2. Open HTTPS URL in browser
  3. Accept the security warning
  4. Click "Start Camera"
  5. Allow camera permission when prompted
  6. Camera feed should appear in the video element
  7. Enable "Auto-capture" to test API integration

🔧 TROUBLESHOOTING

  "Camera access not accepted"
    → Ensure URL starts with https://
    → Try in incognito/private mode
    → Clear browser cache

  "Port 8000 in use"
    → Use different port: python manage.py runserver_plus 0.0.0.0:8001

  "Cannot read properties of undefined"
    → Reload page
    → Check browser console (F12)
    → Check that HTML element IDs match

  Camera shows but no recognition
    → Check API URL in settings matches server IP
    → Check server logs for API errors
    → Check browser console for JavaScript errors

📂 IMPORTANT FILES

  certs/localhost.crt        SSL certificate
  certs/localhost.key        SSL private key
  face_frontend.html         Frontend app
  FRONTEND_SETUP.md          Detailed setup guide
  CAMERA_SETUP.md            Camera troubleshooting
  generate_cert.py           Certificate generator
  run_https_server.ps1       PowerShell startup script
  run_https_server.bat       Command Prompt startup script

🌐 API ENDPOINT

  The frontend sends frames to:
    POST /user_faces/recognition/
  
  With form data:
    - image: JPEG image file
    - serviceId: (optional)

📚 DOCUMENTATION

  Full setup guide:
    → FRONTEND_SETUP.md
  
  Camera-specific troubleshooting:
    → CAMERA_SETUP.md
  
  This summary:
    → CAMERA_FIX_SUMMARY.md

╔════════════════════════════════════════════════════════════════╗
║  Ready to use! Run the startup script and open the HTTPS URL   ║
╚════════════════════════════════════════════════════════════════╝
